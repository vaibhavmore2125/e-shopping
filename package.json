
{
  
  "devDependencies": {
  
    
  "concurrently": "^9.2.0"
  
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
     "start": "concurrently \"cd backend && npx nodemon index.js \" \"cd frontend && npm run dev \""
  }
 }

